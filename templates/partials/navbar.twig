<nav class="nav" aria-label="Main navigation">
    <button class="nav-toggle" aria-expanded="false" aria-controls="nav-menu">
        <span class="sr-only">Menu</span>
        <div class="hamburger">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </button>
    
    <div id="nav-menu" class="nav-menu">
        <a href="/" onclick="window.dispatchEvent(new Event('nav:close'))">Home</a>
        <a href="/tickets" onclick="window.dispatchEvent(new Event('nav:close'))">Tickets</a>
        <a href="/dashboard" onclick="window.dispatchEvent(new Event('nav:close'))">Dashboard</a>

        {% if not isAuthenticated() %}
            <a href="/auth/login" onclick="window.dispatchEvent(new Event('nav:close'))">Login</a>
            <a href="/auth/signup" onclick="window.dispatchEvent(new Event('nav:close'))">Sign Up</a>
        {% else %}
            <form method="POST" action="/auth/logout" style="display:inline">
                <button type="submit" class="logout-btn" onclick="window.dispatchEvent(new Event('nav:close'))">Logout</button>
            </form>
        {% endif %}
    </div>
</nav>