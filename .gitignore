# Git ignore for Twig/PHP ticket app

# Composer vendor directory
/vendor/

# Composer setup
/composer-setup.php

# Environment files (local secrets)
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db

# Editors / IDEs
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# PHPStorm metadata
.phpstorm.meta.php

# Node dependencies
node_modules/

# Logs, caches, and runtime files
logs/
cache/
tmp/
*.log
*.cache

# PHPUnit cache
.phpunit.result.cache

# Build / distribution
dist/
build/

# Generated / bundled assets (source maps etc.)
public/assets/*.map
public/assets/*.hot-update.js

# Local runtime data: the repo contains sample JSON files in data/; do not ignore them by default
# If you want to keep local-only copies out of git, create data/*.local or add specific patterns here.

# -----------------------------
# Deployment-critical files
# -----------------------------
# Ensure critical files and directories required to deploy the app are tracked.
# These negations are safe (they only re-include paths if a higher-level pattern ignores them).
!/.gitignore
!/composer.json
!/composer.lock
!/public/
!/public/assets/
!/src/
!/templates/
!/assets/
!/data/
!/data/*.json

# If you prefer to commit vendor/ instead of running `composer install` on the server,
# remove the `/vendor/` line above. By default vendor/ remains ignored here.
